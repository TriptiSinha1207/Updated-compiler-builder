services:
  - type: web_service
    name: ai-signal-backend
    env: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: PYTHONPATH=. gunicorn --bind 0.0.0.0:$PORT server:app
